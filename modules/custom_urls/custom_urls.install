<?php

/**
 * Implements hook_uninstall().
 */

function custom_urls_uninstall() {
  // Delete the taxonomy created... Users will have to remove the content type manually
  $taxonomy = \Drupal::configFactory()->getEditable('taxonomy.vocabulary.application_status_codes');
  if($taxonomy){
      $taxonomy->delete();
  }
}